# Build Status

Descriptive tracker of what is *designed* vs. *actually built*. This file drifts fast and is deliberately separate from the normative design (CORE) so build churn never pollutes the spec. Update whenever implementation state changes, and log the change in `CHANGELOG.md`.

**Status values:** `Designed` (in CORE, not started) · `In progress` · `Built` (working) · `Validated` (meets the relevant success criterion in CORE §13).

## By roadmap phase

CORE §10 defines the phases and *why* they're ordered this way (dependency chain — the deterministic core must exist before anything can be honest or consistent).

| Phase | Component(s) | Status | Notes |
|---|---|---|---|
| 1 | Deterministic core + event log | Built | `src/fable_table_engine/`: Event model + append-only `EventLog` (assigns sequence/id/timestamp; refuses mechanical types without the rules/dice capability), `DiceService`, minimal `RulesEngine` (3d6+Skill vs TN → FABLE band, `uploads/FABLE_Engine_Schema_v6.md` §5), `WorldState` skeleton, and read-time audience/visibility projection (D-001 seed). `SQLiteEventLog` (drop-in persistent subclass) and `open_session` factory added. All 6 acceptance contracts pass (`tests/test_phase1_contracts.py`); 85 tests total. |
| 2 | Access model + fact-extraction/commit | Built | `access.py`: `CommitPipeline` (sanctioned commit path; canon consistency-check before bind, CORE §6.1–6.2), canon ledger + committed-facts as pure folds over the log (D-009 Resolved), structural contradiction detection against *revealed* canon, and the logged `override` escape hatch (D-008 Resolved). D-007 Resolved: hard cold/warm split decided; current code follows Option B (structured commitment blocks). 11 tests (`tests/test_phase2_access.py`). |
| 3 | Perception model | Built | `perception.py`: `Scene` (volatile sensory conditions — lighting, open/closed connections) over the `WorldState` zone graph (zones + adjacency + presence + intra-zone closeness Truths, all added to `world_state.py`). `perceivers`/`perception_map` compute who could sense a `Stimulus` (auditory by volume: whisper=close-only/in-zone, normal=zone, loud=+one open hop; visual=lit + line of sight); `derive_overhears` emits `may_have_perceived` events for unintended perceivers (neutral `perception` author, vague hint, `derived_from` the source). 12 functional + 14 adversarial stress tests. Thin first cut (D-012) — fiction-positional (D-002), no coordinates. Richer propagation deferred (D-012). |
| 4 | Context assembly | Built | `context.py`: `ContextAssembler.belief_store(pov)` → `BeliefStore` (the POV's projected events + `beliefs` folded only from commitments it saw at content level — never the global canon, so revealed-elsewhere facts don't leak — + ambient `perceptible` entities via the perception model when a `Scene` is supplied). Belief stores are read-time projections (D-001), derived snapshots, never authoritative. D-013 Resolved: `project_for` now emits a per-POV contiguous index, closing the global-`sequence` side-channel. 9 tests (`tests/test_phase4_context.py`). Omits until later phases: persona (6), disposition (10), memory. |
| 5 | Cold/warm GM split | Built (in-memory + API) | `character_sheet.py`: `CharacterSheet` (skills 0–4, edge, stress). `gm.py`: `AdjudicatorGM` (cold; forces `adjudicate_action` tool call via Anthropic SDK; returns `StakesDecision`), `NarratorGM` (warm; prose only; never receives dice values), `WorldSimulator` (deterministic clock tick; emits `front_advance` events). `beat.py`: `BeatRunner` — steps 2–9 of the beat loop for one actor; `DeliveryScope` (resolved at beat entry from `Proposal.channel`/`target`); OOC bypass; whisper target validation; narrator context filtered to public-channel events for public beats (D-033). 23 tests (`tests/test_phase5_gm.py`) + 28 audience-preservation tests (`tests/test_audience_preservation.py`); 294 total. Provider: Anthropic SDK, `claude-sonnet-4-6` default. |
| 6 | Character agents | Built | `persona.py`: `PersonaSpec` (voice, values, public/hidden goals, relationships). `character_agent.py`: `Proposal` (seat-agnostic action/dialogue container; D-015 groundwork), `CharacterAgent` (AI-driven seat; reads own filtered belief store via `propose_action` tool call; hidden_agenda in system prompt only). 21 tests (`tests/test_phase6_agents.py`); 129 total. Differential-knowledge tests verify two agents with different audience memberships receive different contexts. |
| 7 | Orchestrator / spotlight | Built | `orchestrator.py`: `ActionQueue` (transient, non-authoritative proposal buffer; D-010), `Orchestrator` (SPOTLIGHT — least-recently-acted; INITIATIVE — caller-supplied round-robin; metadata-only routing; never reads event content). `beat.py` extended: `BeatRunner.run_with_agent` and `BeatRunner.run_round` (full round; AI seats call `run_with_agent`, human seats use `player_proposals`; D-015 seat-agnostic). 43 tests (`tests/test_phase7_orchestrator.py`); 172 total. |
| 8 | Auditor | Built | `auditor.py`: `AuditTier` (CRITICAL/NON_CRITICAL/ADVISORY), `AuditFlag`, `AuditResult`, `Auditor`. Pre-commit hook (canon-contradiction detection; D-008 override passthrough) + post-narration hook (structural + optional semantic model call; D-019 escalation). `BeatResult.audit_flags` + `beat_aborted`. Audit events carry `audience=(gm_entity,)` only. 35 tests (`tests/test_phase8_auditor.py`); 207 total. |
| 9 | Audience-preserving delivery contract | Built | `DeliveryScope` frozen dataclass resolved at beat entry from `Proposal.channel`/`target`; threaded to step-9 log.append — nothing downstream can widen or reconstruct the audience. `_resolve_delivery()` validates whisper targets before any model call. OOC bypass (no adjudicator/narrator/commit/clock calls). `_narrator_context()` filters to public-channel events for public beats. `run_with_agent()` passes channel/target. `run_round()` accepts `str \| Proposal`. `BeatResult.channel` added. D-033 resolved. 28 adversarial tests (`tests/test_audience_preservation.py`); 294 total. |
| 10 | Atomic session + replayable scene state | Built | `event_log.py`: `EventLog.transaction()` no-op added so `BeatRunner` always calls it regardless of backend. `persistence.py`: `SQLiteScene` (persists `dark_zones` + `closed_connections`; shares `_tx_active` flag; rollback calls `_load()` via `SQLiteEventLog._scene_ref`; adds `illuminate()` and `open_connection()` mutation methods); `open_session()` now returns `(log, world, scene)` 3-tuple. `beat.py`: `_BeatAborted` exception; steps 6–9 (fact commit → narrate → post-narration audit → narration log → clock ticks) wrapped in `with self._log.transaction():`; post-narration audit block raises `_BeatAborted` triggering rollback rather than an early return, ensuring fact commits are undone atomically. 17 tests (`tests/test_phase10_session.py`); 312 total. |
| 11 | Epistemic commitment contract | Built | `Commitment` gains `epistemic_type` ("fact"/"claim"/"observation"), `asserting_entity`, `observing_entity` (D-024). `committed_facts()` folds only facts (claims no longer overwrite facts). `BeliefStore` gains `claims` + `observations` tuples; `believes()`/`value_of()` operate on facts only. `ContextAssembler._fold_epistemic()` splits projections into all three categories. `_commitment_from_dict()` round-trips provenance fields. 33 tests (`tests/test_phase11_epistemic.py`); 345 total. |
| 12 | Typed effect executor | Built | `effects.py`: `EffectExecutor` + 10 typed effect operations (CreateTruth, ChangeTruth, ExpireTruth, AdvanceClock, ApplyStress, ChangeAccess, MoveEntity, ChangeResource, CreateMaintainedTruth, ExpireMaintainedTruth). `"expired"` epistemic type added as tombstone; `committed_facts()` and `canon_ledger()` handle expiry. `Scene` gains `illuminate()` / `open_connection()` on base class. `WorldState` gains `maintained_truths`, `update_entity()`, `set_maintained_truth()`, `expire_maintained_truth()` (all persisted in SQLite). `BeatRunner` gains optional `executor` param; step 6 routes through executor when present, falls back to direct pipeline.commit. 53 tests (`tests/test_phase12_effects.py`); 398 total. |
| 13 | Narrow complete FABLE resolution slice | Built | `ResolutionPlan` (formerly `StakesDecision`) + consequence palette (D-025): `gm.py` extended with `action_domain`, `exposure`, `effect`, trade fields, `consequence_palette`, `triumph_effects`, `edge_label`, `seam`. `effect_from_dict()` + `describe_effect()` added to `effects.py`. `BeatRunner.run()` gains `trade` param; `_apply_trade()` adjusts exposure/effect tier (never TN — D-025 invariant); palette effects applied inside transaction via executor, conversion/application failures log `audit_advisory` and never abort a resolved beat. `simulator.advance()` now receives `stakes.action_domain` (D-026 domain tag). `NarratorGM.narrate()` receives `effective_effect` and `applied_summary`. `BeatResult` gains `applied_trade`, `effective_exposure`, `effective_effect`. `StakesDecision = ResolutionPlan` alias preserves backward compatibility. 58 tests (`tests/test_phase13_resolution.py`); 456 total. |
| 14 | Provider gateway + isolated telemetry | Built | `provider.py`: `ModelGateway` (single model-call seam; `call(role, **kwargs)` wraps `client.messages.create`, records telemetry, returns response), `TelemetrySink` (in-process store of `CallRecord` entries; `summary()` by role; zero coupling to fictional state — D-022), `CallRecord` (role, model, tokens, cost_usd, latency_ms). All four callers (`AdjudicatorGM`, `NarratorGM`, `CharacterAgent`, `Auditor`) accept `ModelGateway` instead of raw client. Pricing table for sonnet/haiku/opus (2026-06-04). 26 tests (`tests/test_phase14_gateway.py`); 482 total. D-022 resolved. D-017 open (provider-agnostic adapter deferred). |
| 15 | Human-seat adapter + text playtest console | Built | `console.py`: `parse_proposal` (text → `Proposal`; whisper/OOC syntax; ValueError on malformed input), `render_event` (entitled event → display string or None; narration/ooc/dice/resolution/front_advance; GM-internal types silenced), `PlaytestSession` (wraps `BeatRunner`; `step(text)` returns new entitled lines per call; `player_view()` full history; `export_transcript()` / `export_transcript_json()` — reads only `belief_store(player_id)`, never raw log). Human and AI proposals use the same `Proposal` → `run()` contract. Architecture invariant: client never computes audiences/rules/effects/hidden state. 44 tests (`tests/test_phase15_console.py`); 526 total. |
| 16 | Scene cadence + companion activation | Built | `orchestrator.py`: `SceneMode` (6 narrative modes), `SceneCadence` (holds mode + always-active set; `select_companions` gates companions with spotlight-priority ordering; mode transitions are deterministic — no model calls), `Orchestrator.sorted_by_spotlight` (least-recently-acted ordering for slot assignment). `beat.py`: `BeatRunner.run_round` gains optional `scene_cadence` param; gated companions removed from rotation and receive zero model calls; human seats never gated. D-021 resolved (option b). 46 tests (`tests/test_phase16_cadence.py`); 572 total. |
| 17 | Campaign package + plot-graph core | Built | `campaign.py`: `CampaignPackage`, `load_campaign`, `load_campaign_dict` with cross-reference validation. `plot_graph.py` extended: `update_hook_binding` mutation seam, `add_hidden_node`, `to_dict`/`from_dict` serialization. `persistence.py`: `SQLitePlotGraph` (persists all mutations within D-023 transaction model; `_plot_graph_ref` on `SQLiteEventLog` wires rollback), `attach_campaign` factory. `schemas/campaign.schema.json` added. `PlotManager.accept_rebinding` routes through `update_hook_binding`. D-037 resolved. 31 tests (`tests/test_phase17_campaign.py`); 606 total. |
| 18 | Plot-manager runtime | Built (validated against Phase 17) | *Formerly old Phase 9.* `plot_graph.py` + `plot_manager.py`: `PlotManager` (sole plot-graph writer; D-016 resolved), fixture-health detection, propose/accept re-binding, `post_beat`, `gm_context_summary`. D-026: `WorldSimulator.advance(trigger)` clock trigger filtering. D-023: `SQLiteEventLog.transaction()` context manager. 59 tests (`tests/test_phase9_plot.py`); 266 total. `accept_rebinding` now routes through `PlotGraph.update_hook_binding`, so `SQLitePlotGraph` intercepts and persists rebindings. Phase 18 is complete as a self-contained unit; the pre-Phase-17 schema note is resolved. |
| 19 | Disposition graph core | Built | `disposition.py`: `DispositionAxis` (4 axes), `DispositionDelta` (causal_event_id required), `DispositionGraph` (directed multi-axis edges, `apply_delta`, `context_block`, `to_dict`/`from_dict`), `DispositionEngine` (sole authoritative writer; deterministic rule table: `disposition_delta`, `stress_taken_for`, `triumph_for` predicates). `persistence.py`: `SQLiteDispositionGraph` (persists all deltas within D-023 tx model; rollback via `_disposition_ref`), `attach_disposition` factory. D-011 deterministic half resolved; model-proposed deltas deferred to Phase 20. 37 tests (`tests/test_phase19_disposition.py`); 643 total. |
| 20 | Social interpretation + Bond compels | Built | `social.py`: `SocialInterpreter` (model tool calls: `propose_social_delta` + `propose_compel`; validates proposals; interiority screening), `PendingCompel` (frozen; waiting for player accept/refuse), `CompelResolution`, `resolve_compel()` (authoritative write point: `compel_accepted`/`compel_refused`/`compel_resolved` events + `GainEdge` via executor). `character_sheet.py`: `BondRef` frozen dataclass (stable Bond→Commitment link), `CharacterSheet.bond_refs`. `effects.py`: `GainEdge` (cap-3), `SpendEdge` (lean/push/shield). `provider.py`: `ModelCallError`, timeout + 1-retry + backoff in `ModelGateway.call()`. D-011 fully resolved. 52 tests; 695 total. |
| 21 | Production text-channel API + interface MVP | **Built** | *Formerly old Phase 11 — voice/TTS scope removed.* Deliverable 1: schema version guard + session manager (22 tests). Deliverable 2: Edge mechanic Lean/Push/Shield (28 tests). Deliverable 3: D-027 action lifecycle state machine (30 tests). Deliverable 4: D-029 roll visibility (29 tests). Deliverable 5: D-031 correction/retcon (24 tests). Deliverable 6: D-032 epistemic certainty labels (36 tests). Deliverable 7: D-028 knowledge transfer enforcement (26 tests). Deliverable 8: D-030 time anchor + scene transition (47 tests). Deliverable 9: D-041 settings system (58 tests). Deliverable 10: home screen + play interface (64 tests) — `HomeScreen` (campaign/session listing + rendering), `PlayInterface` (submit/history/render_settings/render_status; never accesses log directly), `build_play_interface` factory (wires executor/auditor/simulator/plot_manager/budgeter/lore_assembler). Security tests verify GM-private events absent from player history. `WorldState.zone_of()` bugfix: returns `None` for unknown entities instead of raising `KeyError`. 1085 total tests. |
| 22 | Beta hardening + release gate | In progress | Items 1–4 (D-017, D-042, D-043) built; lorebook injected into all three prompt paths; cost-ceiling in `render_status()`; save-format migration registry (`_MIGRATION_REGISTRY`, `_apply_migrations`, ENGINE_SCHEMA_VERSION=22.0, auto-migrates 21.3 sessions); `event.schema.json` updated (Phase 11 epistemic fields, D-029 roll_visibility, D-031 authorized_by); golden transcript suite (53 tests: stakeless/stakes beats, fact commits, effect application, audience separation, OOC, multi-beat, SQLite round-trip). 1309 total tests. Remaining: replay/fuzz/property tests, security review of client payloads. |

## FABLE Engine Schema v6 — implementation coverage

Cross-reference against `uploads/FABLE_Engine_Schema_v6.md`. Built = deterministic code implements and enforces this surface. Partial = some code exists but is not fully enforced. Missing = not yet implemented. Phase assignments are the earliest phase where the gap is expected to close.

| §  | Mechanic | Status | Notes / Phase |
|---|---|---|---|
| §5 | 3d6 + Skill vs TN, 4 bands (Triumph/Success/Cost/Setback) | **Built** | `rules.py`, `band_for_margin` — Phase 1 |
| §5 | No-empty-rolls rule / Exit Check gate | **Built** | Adjudicator tool schema gates on stakes — Phase 5 |
| §5 | Exposure, Effect, Trade (Aggressive/Measured/Guarded, pre-roll) | **Built** | `gm.py` ResolutionPlan, `beat.py` `_apply_trade` — Phase 13 |
| §6 | TN table (8/10/12/13/14; contested = 10+Skill) | Partial | Values in adjudicator prompt, not enforced by rules engine — Phase 22 |
| §7 | Exposure (0–2) | **Built** | In ResolutionPlan — Phase 13 |
| §7 | Cost registers (Harm/Time/Ground/Trace/Resource/Relational) | Partial | Harm (`ApplyStress`), Time (`AdvanceClock`), Resource (`ChangeResource`), Access/Seam (`ChangeAccess`) built. Ground/Trace/Relational have no typed effects — Phase 22 |
| §8 | Effect tiers (Limited/Standard/Great/Spectacular) | **Built** | Effect tiers in ResolutionPlan — Phase 13 |
| §10 | Ledger (Position → −Exposure; Leverage → +Effect; Access; Seam) | Partial | Adjudicator-managed; Exposure/Effect adjustments flow through ResolutionPlan. No deterministic enforcement that a claim pays only once — Phase 22 |
| §11 | Exit Check (Bottom/Top/Live Roll); simple vs. complex obstacles | **Built** | Adjudicator tool schema includes `has_stakes` boolean and `action_domain` — Phase 5/13 |
| §12 | Standing Truths (persist until play changes them) | **Built** | `CreateTruth`/`ChangeTruth`/`ExpireTruth` in EffectExecutor — Phase 12 |
| §12 | Maintained Truths (lapse on next world-motion unless renewed) | **Built** | `CreateMaintainedTruth`/`ExpireMaintainedTruth` — Phase 12 |
| §12 | Recovery clocks (opposition countdown to lapse a maintained Truth) | Missing | No `recovery_for` metadata on clock schema; just a regular clock. Add `recovery_for: str \| None` to clock schema — Phase 22 |
| §13 | Edge cap 3, gain on Triumph | **Built** | `GainEdge` typed effect in `effects.py`; cap-3 enforced in `EffectExecutor._gain_edge()` — Phase 20 |
| §13 | Edge gain on compel / costly expression | **Built** | `resolve_compel()` always applies `GainEdge(1)` on accept via executor — Phase 20 |
| §13 | Edge spend — currency side (deduct Edge, enforce minimum) | **Built** | `SpendEdge(entity_id, amount, spend_type)` in `EffectExecutor`; insufficient-Edge rejected; spend_type logged for provenance — Phase 20 |
| §13 | Edge spend — mechanic side (Lean steps band; Push costs 2 Stress; Shield redirects Harm) | Missing | Beat loop has no Edge-spend prompt, no band-step, no Push Stress cost, no Shield redirect. `spend_type` is a provenance tag only — the beat ignores it. Phase 21 (Lean minimum before live play) |
| §14 | Stress track (6 boxes, cleared by genuine breather) | Partial | 6-box cap (`STRESS_CAP=6`) enforced in `EffectExecutor._apply_stress`; breather-clear not yet a typed effect — Phase 22 |
| §14 | Stress overflow → exactly 1 Scar (then clear Stress) | **Built** | Overflow cascade built: stress cleared to 0, `ApplyScar(via_overflow=True)` auto-applied; scar cap rejection surfaces to caller — pre-Phase-21 pull-forward |
| §14 | Scars (3 slots: Wound/Mark/Loss; situational, not global) | **Built** | `ApplyScar` typed effect with 3-slot cap (`SCAR_CAP=3`); `character_broken` event at cap; stored on `entity.resources["scars"]` — pre-Phase-21 pull-forward |
| §14 | Scar route invariant (only Stress overflow or live Seam terminal) | **Built** | Enforced in `EffectExecutor._apply_scar`: rejected if neither `via_overflow=True` nor `seam_event_id` is set. Live-Seam validation (is it actually active?) deferred to Phase 22 |
| §15 | Seam (vulnerability enabling terminal consequence) | Partial | `ChangeAccess` covers Access; no `CreateSeam` typed effect; terminal consequence not enforced by rules engine — Phase 22 |
| §16 | Clocks (named trigger, landing Truth, 4/6/8 sizes) | **Built** | Clock schema with `trigger_types`, `active`, `domain`, `landing_truth`, `fired` — Phases 1/13 |
| §16 | Recovery clock as a distinct clock type | Missing | See §12 note above — Phase 22 |
| §17 | Fronts (Agenda, Standing Truth, Clocks, Levers, Seams) | Partial | Front has `name`/`threat`/`clock_name`/`consequence_truth`/`faction_id`. Levers and Seams on Fronts not modeled — Phase 18/22 |
| §18 | Prep Rounds (one prep action per character; Front/mission clock ticks at round end) | Missing | No PrepRound mechanism — Phase 22 |
| §19 | Opposition classes (Obstacle/Minor/Significant/Front) | Missing | No formal opposition model; adjudicator-managed — Phase 22 |
| §20 | Volatile overlay (minimum Exposure 1; can't Top Exit; result table overlay) | Missing | Not implemented — Phase 22 |
| §21 | Advancement (causal accomplishment triggers; Skill/Trait/Bond spend) | Missing | Not implemented — Phase 22 |
| §22 | Mode rule (Modes = configurations of primitives, not new subsystems) | Policy only | `SceneMode` is cadence/pacing, not a FABLE Mode. Mode-as-constraint not enforced — Phase 22 |
| §23 | Invariants 1–18 (TN measures difficulty only; Exposure severity only; etc.) | Partial | Core invariants (1–3, 5, 8, 9, 11, 12, 13) are enforced structurally. Invariant 9 (Scar route) now enforced deterministically. Invariants 10 (maintained lapse), 14 (opposition via clocks), 17 (advancement causal only) not yet enforced — Phase 22 |

**Key gaps before live play:** Lean/Push/Shield beat integration (§13 mechanic side — band-step, Push Stress cost, Shield redirect — Phase 21); breather-clear for Stress (Phase 22); live-Seam validation on `ApplyScar` (Phase 22). Edge economy (cap/grant/spend currency), Stress cap/overflow, Scars, and Scar route invariant are built.

---

## Known schema drift (as of 2026-06-18)

- **`schemas/event.schema.json`**: Updated in Phase 22 to include Phase 11 epistemic fields (`epistemic_type`, `asserting_entity`, `observing_entity` on `Commitment`), D-029 `roll_visibility`, and D-031 `authorized_by`. Schema validation round-trip tests are a remaining Phase 22 deliverable.
- **SQLite session DB**: `schema_version` table + `_MIGRATION_REGISTRY` added in Phase 22. Sessions stamped at any known version are auto-migrated on open. Unknown/future versions raise `SchemaVersionError`.

---

## Existing substrate (starting point, not yet to spec)

- **FABLE_AI_Engine** — FastAPI + SQLite + React/TypeScript. Closest existing base for the deterministic core and interface, but predates this blueprint's access model and GM decomposition.
- **Playtest harness** (`momentum-table` lineage) — Python, director-pattern routing via `[SPOTLIGHT:]` tags, per-character ElevenLabs TTS, browser GUI. Source of the orchestration and voice patterns; not built around belief stores or the determinism boundary.
- **GUI mockup** — `uploads/fable_mockup/` — React/JSX prototype of the full player interface ("The Salt Lantern" scenario). Playable in a browser. Contains backend-relevant content: delivery channel model (6 IC delivery types mapping to backend channels), REACH table (zone × delivery reachability), Private GM Query (gmq) channel (not yet in backend), character presence states (`present / separated / hidden / incapacitated / unavailable`), CASEFILE category grouping (People / Places / Factions / Known Truths / Clues / Open Questions / Promises & Debts), action-resolution card fields (matches D-025 `ResolutionPlan`), frontend event protocol (`kind: gm/ally/npc/system/dice/beat/tick/stat/spotlight/recent`), MAINTAINED items (held truths with lapse conditions), and SETTINGS_JSON session config. See `memory/reference_gui_mockup.md` for the full inventory. This substrate informs the channel router design for phase 11.

These are *substrate*, not compliance — where they conflict with CORE, CORE is the target.

## Validation against CORE §13 success criteria

| Criterion | State |
|---|---|
| Secret-keeping (whisper never leaks to non-audience) | Substantially covered — projection secrecy (2) + perception's who-could-sense with no content/identity leak (3, stress-tested) + belief stores that exclude non-audience content and derive beliefs only from what the POV saw (4) + the `sequence` side-channel closed (D-013) + **D-033**: whisper proposals now resolve to a `DeliveryScope` at beat entry; step-9 narration event carries `channel="whisper"` and `audience=(actor, target, gm)` only; narrator context for public beats filtered to exclude actor-private events; OOC bypasses all fiction; 28 adversarial tests pass. Live narration-vs-state audit still pends the auditor (8). |
| Spatial/causal consistency (no interaction across committed distance without traversal) | Partial — canon contradiction of a committed distance is blocked (phase 2), and perception refuses sensing across a closed/non-adjacent connection (phase 3). Live enforcement against proposals still needs the auditor (8). |
| No over-rolling (stakes gate) | Testable and exercised — `BeatRunner` routes through `AdjudicatorGM.evaluate`; no-stakes beats produce no dice events; stakes beats produce `dice_roll` + `resolution` events. Verified by phase 5 tests. Full live audit pends the auditor (8). |
| Invisible improvisation (revisions never contradict canon ledger) | Not yet testable — needs phase 9. |
| Differential believability (teammates act on private info) | Substantially demonstrated — two agents with different audience memberships receive different contexts (phase 6 tests); `run_with_agent` drives each agent from its own filtered belief store through a full beat (phase 7 tests). Auditor live-gate on output pends phase 8. |
| Responsiveness (per-beat budget) | Budget not yet set — see D-005. |
